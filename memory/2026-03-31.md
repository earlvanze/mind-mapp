# 2026-03-31 - Mind Mapp Session

## What happened
- **Tests:** 568/568 passing ✅
- **Build:** succeeds cleanly ✅
- **Commits:** 2 (8f70fda + d2dbac6)

### Commits
1. `8f70fda` feat: add /regex/i search support (from prior session)
2. `d2dbac6` feat: wire handlers prop + onVersionHistory into useKeyboard — enables custom shortcut routing

### Fix: handlers prop wiring
The custom shortcut system had infrastructure in place (`checkCustomBinding`, `ACTION_TO_HANDLER`, `getHandlerNameForAction` in `useKeyboard.ts`) but App.tsx never passed the `handlers` prop, so custom shortcuts had no effect. Fixed by:

1. Passing `handlers={...allCallbacks}` to `useKeyboard()` in App.tsx
2. Adding missing `onVersionHistory: () => toggleVersionHistory()` prop

This means custom keyboard shortcuts from ShortcutSettingsDialog now actually dispatch to the right handlers.

### Backlog Status
- [x] Keyboard shortcut customization (partially done — wiring now complete, UI in ShortcutSettingsDialog)
- [x] Advanced filters (shape, color, icon, date) ✅
- [x] Node tags/categories ✅
- [x] Regex search (`/pattern/flags`) ✅

### Still on Future backlog
- Collaborative editing (CRDT-based)
- Plugin system for custom visualizations
- Mobile app (React Native)
- File attachments (non-image binary files)
- Export templates (custom styling presets)
- Cloud sync (optional backend)
- Node animations and transitions

### State
- Branch: feature/node-tags-merged
- Commits: 8f70fda + d2dbac6
- Version: v0.13.x
