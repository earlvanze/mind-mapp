
## 2026-03-09 — Mind Mapp sprint durable notes

- Completed high-leverage product task: implemented **undo/redo history** end-to-end in Mind Mapp.
  - Store now tracks `past`/`future` snapshots, `canUndo`/`canRedo`, and `undo()`/`redo()`.
  - Added bounded history (`MAX_HISTORY=100`).
  - Integrated history commits into mutating actions (set text, add, delete, promote, auto-layout, import, reset).
  - Drag behavior now avoids history spam by committing once on mouseup.

- Added keyboard shortcuts and controls for history:
  - `Cmd/Ctrl+Z` = undo
  - `Cmd/Ctrl+Shift+Z` or `Cmd/Ctrl+Y` = redo
  - Added Undo/Redo toolbar buttons with disabled state handling.

- Strengthened import flow (recent sprint):
  - Added import validation utility with shape/version checks and user-facing success/error notices in toolbar.
  - Current version support is strict to schema version `1`.

- Documentation and release artifacts were kept in sync across sprint:
  - Updated `CHANGELOG.md`, `STATUS.md`, `KEYS.md`, `RUN.md`, `FAQ.md`, `IMPORTS.md`.
  - Rebuilt `dist/` after feature changes.

- Operational blocker remains:
  - Office accomplishment API calls continue failing with auth errors (missing/invalid token) or connection failure.

- Recommended next engineering priorities:
  1. Import integrity phase 2 (parent/child consistency, orphan/cycle checks).
  2. Minimal tests for import parser + undo/redo behavior.
  3. Multi-select + group move.

## 2026-03-09 — Context isolation correction

- Autonomous Ops Studio website work (`site/index.html`, `site/audit.html`) is a separate project and must stay under:
  - `/data/.openclaw/workspace/Projects/zero-human-company`
- Mind Mapp scope remains isolated under:
  - `/data/.openclaw/workspace-tech-mvp/projects/mind-mapp/app`
- Do not mix Autonomous Ops Studio tasks into Mind Mapp changelogs, commits, status reports, or assumptions.
