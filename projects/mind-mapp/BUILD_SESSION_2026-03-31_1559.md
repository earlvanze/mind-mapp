# Build Session — 2026-03-31 15:59 UTC

## Status: ✅ Complete

### Tests
- **530 tests passed** (56 test files)
- +4 new tests in `searchRegex.test.ts`
- No regressions

### Commits
1. `8f70fda` feat: add /regex/i search support

### Actions Taken
1. Ran full test suite — all pass (530/530)
2. Implemented `/regex/i` search syntax (e.g., `/(?:node|leaf)/i`, `/urgent/`, `/^root$/i`)
3. Added `searchRegex.test.ts` with 4 test cases
4. Updated search placeholder to hint at new syntax
5. Added `termMatches()` helper to cleanly route regex/wildcard/literal matching
6. Updated `PartitionedSearchTerms` type with `hasRegexTerms`
7. Updated ranking logic for regex (label > id > path)

### New Feature: Regex Search
- Syntax: `/pattern/flags` (e.g., `/node|leaf/i` for case-insensitive OR match)
- Supports flags: `i` (case-insensitive), `g` (global), `m` (multiline), `s`, `u`, `v`, `y`
- Works alongside existing wildcards (`*`, `?`) and negation (`-term`)
- Invalid regex falls back gracefully (returns false, no crashes)
- Combined query example: `urgent /deadline|overdue/i -misc`

### Branch
`feature/node-tags-merged` (local only)

### TSC Status
Pre-existing errors in unrelated files (nodeFilters.test.ts, shareLink.ts, etc.) — not introduced by this session.

